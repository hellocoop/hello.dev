import React from 'react';

const WarningIcon = () => {
  const warningStyle = {
    fontSize: '22px',
    color: 'yellow',
  };

  return (
    <span style={warningStyle}>⚠️</span>
  );
};

export default WarningIcon
